import {css} from "lit";

export const typeface = css`
    :host {
      --md-ref-typeface-plain: Roboto, sans-serif; /* Plain typeface */
      --md-ref-typeface-brand: Roboto, sans-serif; /* Brand typeface */
      --md-ref-typeface-weight-bold: 700; /* Bold weight */
      --md-ref-typeface-weight-medium: 500; /* Medium weight */
      --md-ref-typeface-weight-regular: 400; /* Regular weight */
    }
`;